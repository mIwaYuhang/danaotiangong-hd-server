"""游戏服务端：为《大闹天宫HD》客户端提供区服列表、账号鉴权与全部玩法接口。

分层结构::

    game_server/
    ├── cli.py          命令行入口：读取配置、组装应用、启动 HTTP 服务
    ├── app.py          组装根：配置 → 存储/静态表 → 业务服务 → 路由 → 分发器
    ├── config.py       配置加载：data/config/*.json → 只读 dataclass
    ├── errors.py       统一异常（请求非法 / 业务失败 / 配置错误 / 静态表错误）
    ├── storage.py      SQLite 存档：连接、事务、版本迁移
    ├── catalog.py      静态表：清单校验、按需加载、NPC 分片合并视图
    ├── protocol/       协议层：URL 解析、参数规格、路由表、请求分发、响应封装、HTTP 传输
    ├── services/       业务层：账号、角色、英雄、法宝、背包/商店、招募、战斗、关卡、邮件、活动、任务、争霸、妖王、副本、
    │                   好友、运镖、小黑屋、神器殿、宝石/矿洞、排行榜、仙盟；玩家目录统一寻址真实玩家与机器人并支持跨玩家读写
    └── tools/          开发工具：Lua 导表生成静态表

所有可调参数都放在 ``server/data/config/`` 目录下的 JSON 中，代码中不保留数值默认值。
"""
