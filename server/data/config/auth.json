{
  "_doc": {
    "ticket_ttl_seconds": "账号票据（SDK 登录后返回的 UserID）有效期，秒；票据在有效期内可重复用于进入区服，便于断线重连",
    "session_ttl_seconds": "游戏会话（进入区服后返回的 Session）有效期，秒",
    "pbkdf2_iterations": "密码存储的 PBKDF2-HMAC-SHA256 迭代次数；客户端上送的是 MD5，服务端再加盐迭代",
    "token_bytes": "票据与会话随机令牌的字节数；URL 安全 Base64 编码后的长度由此推导，用于协议层格式校验"
  },
  "ticket_ttl_seconds": 604800,
  "session_ttl_seconds": 86400,
  "pbkdf2_iterations": 260000,
  "token_bytes": 32
}
